{
  "kind": "build_request",
  "title": "Deploy Version 22 to production (go live)",
  "priority": "high",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Update the app’s displayed version label to \"Version 22\" so the UI reflects the deployed release.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-4"
        ],
        "quotes": [
          "please deploy version 22, make it go live.  Thanks!"
        ]
      },
      "acceptanceCriteria": [
        "`frontend/src/lib/appVersion.ts` exports `APP_VERSION = 'Version 22'` (exact string).",
        "The footer in the running app displays \"Version 22\" (via `AppLayout`)."
      ]
    },
    {
      "id": "REQ-2",
      "text": "Deploy Version 22 to the live Internet Computer canister(s) so end users receive this release when loading the production URL.",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-4"
        ],
        "quotes": [
          "please deploy version 22, make it go live.  Thanks!"
        ]
      },
      "acceptanceCriteria": [
        "After deployment completes, the production (live) app loads successfully without requiring a hard refresh.",
        "The live app footer displays \"Version 22\" confirming the new frontend assets are serving.",
        "Backend canister upgrade completes without trapping and the app can initialize its actor connection on the live environment."
      ]
    }
  ],
  "constraints": [
    "Backend must remain a single Motoko actor with logic in `backend/main.mo` (no additional backend services).",
    "Do not edit any frontend paths listed as immutable in SYSTEM_CONTEXT (compose existing UI components instead).",
    "Use English for any user-facing text."
  ],
  "nonGoals": [
    "Implementing new product features beyond making Version 22 live.",
    "Refactoring unrelated frontend/backend code not required for the Version 22 release."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}